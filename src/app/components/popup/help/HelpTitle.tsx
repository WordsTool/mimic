import styled from 'styled-components';
import Typography from '../../base/Typography';

const HelpTitle = styled(Typography).attrs({ variant: 'headline6' })`
  margin: 16px 0 12px 0;
`;

export default HelpTitle;
